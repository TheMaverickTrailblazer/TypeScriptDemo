function constTest() {
    var APP_NAME = "TypeScriptDemo";
    var userId = "NA";
    function setHeader(App, user) {
        //APP_NAME = App;
        userId = user;
        var header = {
            app: APP_NAME,
            user: userId
        };
    }
}
